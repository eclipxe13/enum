filter:
  excluded_paths:
    - 'tests/'
    - 'vendor/'

build:
  dependencies:
    override:
      - composer remove squizlabs/php_codesniffer friendsofphp/php-cs-fixer psalm/phar phpstan/phpstan --dev --no-interaction --no-progress --no-update
      - composer install --no-interaction
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run --enable-security-analysis
          - command: vendor/bin/phpunit tests/Unit/ --coverage-text --coverage-clover=coverage.clover
            coverage:
              file: coverage.clover
              format: clover
